# 表格渲染功能指南

## 功能概述

为 obsidian.nvim 添加了完整的表格渲染功能，支持 pipe table 格式的美观显示，包括边框、对齐指示器和多种渲染模式。

## 支持的功能

### 表格格式支持
- **Pipe Table 格式**: 支持 GitHub 风格的 pipe table
- **多种对齐方式**: 左对齐、居中对齐、右对齐、默认对齐
- **自动列宽计算**: 根据内容自动调整列宽
- **边框渲染**: 完整的表格边框显示

### 渲染模式
- **padded**: 单元格内容填充到统一宽度（推荐）
- **raw**: 只替换管道符，保持原始内容
- **overlay**: 完全覆盖原始表格
- **trimmed**: 填充模式，但移除多余空格

### 视觉特性
- **边框字符**: 可自定义的边框字符
- **对齐指示器**: 在分隔符行显示对齐方向
- **颜色高亮**: 表头、表行、分隔符的不同颜色
- **自动边距**: 可配置的内边距

## 配置选项

### 基本配置

```lua
require("obsidian").setup({
  ui = {
    table = {
      enabled = true,                    -- 启用表格渲染
      border = {                         -- 边框字符配置
        '┌', '┬', '┐',                   -- 顶部边框 (左、中、右)
        '├', '┼', '┤',                   -- 分隔符边框 (左、中、右)
        '└', '┴', '┘',                   -- 底部边框 (左、中、右)
        '│', '─',                        -- 垂直、水平边框
      },
      border_enabled = true,             -- 启用边框显示
      cell = 'padded',                   -- 单元格渲染模式
      padding = 1,                       -- 单元格内边距
      min_width = 3,                     -- 最小列宽
      alignment_indicator = '━',         -- 对齐指示符
      head = 'ObsidianTableHead',         -- 表头高亮组
      row = 'ObsidianTableRow',           -- 表行高亮组
      filler = 'ObsidianTableFill',       -- 填充字符高亮组
    },
  },
})
```

### 高级配置

#### 边框字符自定义
```lua
border = {
  '╔', '╦', '╗',    -- 双线顶部边框
  '╠', '╬', '╣',    -- 双线分隔符
  '╚', '╩', '╝',    -- 双线底部边框
  '║', '═',         -- 双线垂直、水平边框
}
```

#### 不同渲染模式对比
```lua
-- overlay 模式：完全覆盖原始表格
cell = 'overlay',

-- raw 模式：只替换管道符
cell = 'raw',

-- padded 模式：填充到统一宽度（推荐）
cell = 'padded',

-- trimmed 模式：填充但移除多余空格
cell = 'trimmed',
```

#### 对齐方式配置

表格对齐通过分隔符行定义：

```markdown
| Left | Center | Right |
|:-----|:------:|------:|
| Text |  Text  |  Text |
```

对应的配置：
- `:---`: 左对齐
- `:--:`: 居中对齐
- `---:`: 右对齐
- `---`: 默认对齐

## 渲染效果示例

### 原始 Markdown 表格
```markdown
| Name | Age | City |
|------|-----|------|
| Alice | 25 | New York |
| Bob | 30 | Los Angeles |
```

### 渲染后效果（padded 模式）
```
┌───────┬─────┬─────────────┐
│ Name  │ Age │ City        │
├───────┼─────┼─────────────┤
│ Alice │ 25  │ New York    │
│ Bob   │ 30  │ Los Angeles │
└───────┴─────┴─────────────┘
```

### 带对齐指示器的效果
```markdown
| Left | Center | Right |
|:----|:------:|-----:|
| Text |  Text  | Text  |
```

渲染效果：
```
┌─────┬────────┬──────┐
│ Left │ Center │ Right │
├━━━━━┿━━━━━━━━┿━━━━━━┤
│ Text │  Text  │  Text │
└─────┴────────┴──────┘
```

## 高亮组

### 默认高亮组
- `ObsidianTableHead`: 表头和分隔符行的高亮
- `ObsidianTableRow`: 普通表格行的高亮
- `ObsidianTableFill`: 填充字符的高亮

### 自定义高亮
```lua
hl_groups = {
  ObsidianTableHead = { bold = true, fg = "#7dcfff" },
  ObsidianTableRow = { fg = "#c0caf5" },
  ObsidianTableFill = { fg = "#414868" },
}
```

## 使用场景

### 技术文档
适合技术文档中的数据表格、配置表格等。

### 项目文档
项目说明、API 文档中的表格显示。

### 笔记整理
个人笔记中的数据记录、对比表格。

### 报告文档
包含数据统计、对比分析的文档。

## 性能优化

- **懒加载**: 只在检测到表格时才加载渲染模块
- **缓存机制**: 避免重复解析相同的表格结构
- **增量更新**: 只更新修改的表格部分
- **内存管理**: 及时清理不需要的 extmark

## 故障排除

### 表格不渲染
1. 检查 `enabled = true` 是否设置
2. 确认表格格式是否正确（必须包含分隔符行）
3. 检查是否在 Markdown 文件中

### 边框显示异常
1. 确认终端支持 Unicode 字符
2. 检查字体是否支持边框字符
3. 验证 `border_enabled = true`

### 对齐不正确
1. 检查分隔符行的对齐标记
2. 确认 `alignment_indicator` 设置
3. 验证 `cell` 模式是否适合

### 高亮不显示
1. 检查高亮组是否正确定义
2. 确认颜色终端支持
3. 验证 `head`、`row`、`filler` 配置

## 最佳实践

1. **保持一致性**: 在整个项目中使用一致的表格格式
2. **合理列宽**: 避免过窄或过宽的列
3. **适当对齐**: 根据内容选择合适的对齐方式
4. **简洁样式**: 避免过度装饰影响阅读

## 测试验证

### 自动测试
运行测试脚本验证功能：
```bash
lua test_table.lua
```

### 手动验证
1. 打开包含表格的文档
2. 运行验证脚本：
   ```vim
   :luafile verify_rendering.lua
   ```
3. 检查表格渲染效果

### 测试文档
使用 `table_test.md` 文件进行功能测试。

## 兼容性

- **向后兼容**: 不影响现有的标题和其他 UI 功能
- **多格式支持**: 兼容标准的 pipe table 格式
- **配置灵活**: 可以完全自定义或禁用功能
- **性能友好**: 对大文件的影响最小

这个表格渲染功能让您的 Markdown 表格变得更加美观和易读！