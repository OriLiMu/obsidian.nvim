# 标题缩进功能指南

## 功能概述

为 obsidian.nvim 的标题渲染功能添加了缩进支持，可以让 2-4 级标题在渲染时显示缩进，提高文档的层次感和可读性。

## 缩进规则

### 默认缩进规则
- **H1 (一级标题)**: 无缩进
- **H2 (二级标题)**: 缩进 2 个空格
- **H3 (三级标题)**: 缩进 4 个空格
- **H4 (四级标题)**: 缩进 6 个空格
- **H5 (五级标题)**: 无缩进
- **H6 (六级标题)**: 无缩进

### 配置选项

#### 基本配置
```lua
require("obsidian").setup({
  ui = {
    heading = {
      enabled = true,
      -- ... 其他配置 ...
      indent = true,  -- 启用缩进功能
      indent_levels = {
        [2] = 2,  -- H2 缩进2个空格
        [3] = 4,  -- H3 缩进4个空格
        [4] = 6,  -- H4 缩进6个空格
      },
    },
  },
})
```

#### 自定义缩进
您可以完全自定义各级标题的缩进：

```lua
require("obsidian").setup({
  ui = {
    heading = {
      enabled = true,
      indent = true,
      indent_levels = {
        [1] = 0,   -- H1 无缩进
        [2] = 3,   -- H2 缩进3个空格
        [3] = 6,   -- H3 缩进6个空格
        [4] = 9,   -- H4 缩进9个空格
        [5] = 12,  -- H5 缩进12个空格
        [6] = 15,  -- H6 缩进15个空格
      },
    },
  },
})
```

#### 渐进式缩进（每级增加固定空格）：
```lua
require("obsidian").setup({
  ui = {
    heading = {
      enabled = true,
      indent = true,
      indent_levels = {
        [1] = 0,
        [2] = 2,
        [3] = 4,
        [4] = 6,
        [5] = 8,
        [6] = 10,
      },
    },
  },
})
```

## 渲染效果

### 启用缩进后的效果

#### 原始 Markdown：
```markdown
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
```

#### 渲染后效果：
```
󰲡 一级标题
  󰲣 二级标题
    󰲥 三级标题
      󰲧 四级标题
```

### 不同位置模式的缩进效果

#### Overlay 模式（推荐）
- 缩进空格和图标一起覆盖原始的 # 符号
- 视觉效果最自然

#### Inline 模式
- 缩进空格和图标替换原始的 # 符号
- 保持原有的布局结构

#### Right 模式
- 图标显示在行尾，不影响缩进
- 缩进仅影响标题内容的显示位置

## 配置选项详解

### `indent`
- **类型**: `boolean`
- **默认值**: `false`
- **描述**: 是否启用标题缩进功能

### `indent_levels`
- **类型**: `table<integer, integer>`
- **默认值**: `{ [2] = 2, [3] = 4, [4] = 6 }`
- **描述**: 各级标题的缩进空格数
- **键**: 标题级别 (1-6)
- **值**: 缩进空格数

### 与其他功能的兼容性

缩进功能与其他标题渲染功能完全兼容：

- **图标显示**: 图标会考虑缩进位置
- **背景色**: 背景色会正确覆盖缩进区域
- **边框装饰**: 边框不受缩进影响
- **自定义样式**: 可以同时使用自定义样式

## 使用场景

### 1. 技术文档
适合技术文档的多级标题结构，提供清晰的层次感。

### 2. 学术论文
在学术论文中使用，便于区分不同级别的章节。

### 3. 项目文档
项目文档中常用，提高可读性和导航性。

### 4. 笔记整理
个人笔记中使用，更好地组织信息结构。

## 测试验证

### 自动测试
运行测试脚本验证缩进逻辑：
```bash
lua test_indent.lua
```

### 手动验证
1. 打开包含多级标题的文档
2. 运行验证脚本：
   ```vim
   :luafile verify_rendering.lua
   ```
3. 检查各级标题的缩进是否正确

### 测试文档
使用 `heading_test.md` 文件进行测试，包含所有级别的标题。

## 性能考虑

- 缩进功能不会显著影响性能
- 只在启用时计算缩进
- 使用缓存机制避免重复计算

## 故障排除

### 缩进不显示
1. 检查 `indent = true` 是否设置
2. 确认 `indent_levels` 配置正确
3. 检查 `conceallevel` 设置

### 缩进位置不正确
1. 检查 `position` 设置
2. 验证 `indent_levels` 的值
3. 确认没有其他插件冲突

### 背景色不覆盖缩进区域
1. 检查 `width` 设置
2. 确认 `backgrounds = true`
3. 验证高亮组是否正确定义

## 最佳实践

1. **渐进式缩进**: 每级增加 2-4 个空格
2. **一致性**: 在整个项目中保持一致的缩进规则
3. **可读性**: 避免过大的缩进，影响阅读体验
4. **测试**: 在使用前测试不同配置的效果

## 示例配置

### 推荐配置（技术文档）
```lua
require("obsidian").setup({
  ui = {
    heading = {
      enabled = true,
      icons = { "󰲡 ", "󰲣 ", "󰲥 ", "󰲧 ", "󰲩 ", "󰲫 " },
      position = "overlay",
      foregrounds = true,
      backgrounds = true,
      width = "full",
      indent = true,
      indent_levels = {
        [1] = 0,
        [2] = 2,
        [3] = 4,
        [4] = 6,
        [5] = 8,
        [6] = 10,
      },
    },
  },
})
```

### 简洁配置（个人笔记）
```lua
require("obsidian").setup({
  ui = {
    heading = {
      enabled = true,
      icons = { "📄", "📁", "📂", "📋", "📝", "🗒️" },
      position = "inline",
      indent = true,
      indent_levels = {
        [2] = 2,
        [3] = 4,
        [4] = 6,
      },
    },
  },
})
```

这个缩进功能让您的 Markdown 文档更加美观和易读！